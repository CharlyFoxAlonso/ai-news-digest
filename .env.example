# Non-sensitive configuration
LLM_PROVIDER=gemini
LLM_MODEL=gemini-3.5-flash
RECIPIENT_EMAIL=delfatica777@gmail.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
TIMEZONE=America/Argentina/Buenos_Aires
REPOSITORY=owner/ai-news-digest
DRY_RUN=false

# Secrets: replace locally or configure as GitHub Actions secrets.
LLM_API_KEY=replace-with-gemini-api-key
SMTP_USERNAME=replace-with-gmail-address
SMTP_APP_PASSWORD=replace-with-gmail-app-password
